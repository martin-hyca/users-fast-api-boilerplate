<!DOCTYPE html>
<html lang="en" class="h-100">
<head>
    <title>Bootstrap 5 Menu</title>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    {# my styles #}
        <!-- Custom styles for this template -->
    <link href="{{ url_for('static', path='/sticky-footer-navbar.css') }}" rel="stylesheet">
    <link href="{{ url_for('static', path='/style.css') }}" rel="stylesheet">

</head>
<body class="d-flex flex-column h-100">
{# {{ endpoint_name }} #}

 
<header>
  <!-- Fixed navbar -->
  <nav class="navbar navbar-expand-md navbar-dark fixed-top" style="background-color: #054d61;">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">LT</a>
      {# only show menu if logged in #}
      {% if user %}

      {# collapse hamburger #}
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarCollapse">

        {# left side of the navbar #}
        <ul class="navbar-nav me-auto mb-2 mb-md-0">
          <li class="nav-item">
            <a class="nav-link {% if endpoint_name == '/' %}active{% endif%}" aria-current="page" href="/">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link {% if endpoint_name == 'register' %}active{% endif%}" href="/register">Add New User</a>
          </li>
          <li class="nav-item">
            <a class="nav-link {% if endpoint_name == 'users' %}active{% endif%}" href="/users">Users</a>
          </li>
          <li class="nav-item">
            <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">Disabled</a>
          </li>
        </ul>

        {# right side of the navbar #}
        <ul class="navbar-nav ml-auto mb-2 mb-md-0">
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              {{ user }}
            </a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
              <li><a class="dropdown-item" href="/settings">Change password</a></li>
              <li><a class="dropdown-item disabled" href="#">Another action</a></li>
              <li><hr class="dropdown-divider"></li>
              <li>
                <form action="/logout" method="post" class="form-inline my-2 my-lg-0">
                  <button class="dropdown-item"type="submit">
                    Logout {{ user }}
                  </button>
                </form>
              </li>
            </ul>

          </li>
        {% else %} 
          {# <li class="nav-item">
            <a class="nav-link" href="/login">Login</a>
          </li> #}
        {% endif %}
        </ul>

      </div>

    </div>
  </nav>

</header>


<main class="flex-shrink-0">

  <div class="row justify-content-center">

<div class="col-lg-8">
      
          {% block content %}{% endblock %}
          </div>
  </div>
</main>

    <footer class="footer">
      <div class="container">
        <span class="text-muted">Place sticky footer content here. {{ endpoint_name }}
</span>
      </div>
    </footer>


    <!-- Bootstrap 5 JS Bundle (includes Popper) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>